import React from "react";

import NavBarProfile from "@/components/NavBarProfile";

const profilePage = () => {
    return (
        <div>
            <NavBarProfile defaultTab="Profile" />
        </div>
    );
};

export default profilePage;
